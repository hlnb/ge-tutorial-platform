# Enable legacy peer deps to avoid conflicts
legacy-peer-deps=true
node-linker=hoisted
strict-peer-dependencies=false
auto-install-peers=true

# Specific package resolutions
public-hoist-pattern[]=*rollup*
public-hoist-pattern[]=*vite* 